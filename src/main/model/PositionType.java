package model;

// These are the player's positions where :
// - C is center.
// - PF is power forward.
// - SF is small forward.
// - PG is point guard.
// - SG is shooting guard.

public enum PositionType {
    C,PF,SF,PG,SG;
}
